---
title: 'OneTool'
description: 'A single SDK to connect AI agents seamlessly to all APIs and tools.'
icon: 'hammer'
---

<Frame caption="Handles schema definitions, authentication, and execution processes automatically.">
  <img src="../images/one-tool.svg" />
</Frame>

## SDKs

Empower your LLMs with the Pica AI SDK.

<CardGroup cols={2}>
<Card title="Vercel AI SDK" img="/images/vercel.jpg" href="/sdk/vercel-ai">
  Seamlessly integrate Pica with Vercel AI SDK for enhanced AI capabilities
</Card>

<Card title="Anthropic MCP (Coming Soon)" img="/images/anthropic.png" href="/sdk/anthropic-mcp">
  Leverage Anthropic's Model Control Protocol for advanced agent control
</Card>

<Card title="Vapi (Coming Soon)" img="/images/vapi.png" href="/sdk/vapi">
  Build voice-enabled AI applications with Vapi integration
</Card>

<Card title="Langchain (Coming Soon)" img="/images/langchain.jpg" href="/sdk/langchain">
  Connect Pica with Langchain for powerful chain-of-thought capabilities
</Card>
</CardGroup>

## Quick Start with OneTool

```typescript
import { openai } from "@ai-sdk/openai";
import { convertToCoreMessages, streamText } from "ai";
import { Pica } from "@picahq/ai-sdk";

// ✨ Agent below has access to all QuickBooks, Slack, and Gmail actions

export async function POST(request: Request) {
  const { messages } = await request.json();

  const pica = new Pica(process.env.Pica_SECRET_KEY as string, {
    agentName: "Pica",
    availableTools: ['quickbook', 'slack', 'gmail'],
    accessLevel: 'full',
  });

  // ✨ Your AI automatically gets a system prompt with any caveats
  const system = await pica.generateSystemPrompt();

  const stream = await streamText({
    model: openai("gpt-4o"),
    system,
    tools: [...pica.oneTool,], // ✨ The magic of Pica's OneTool
    messages: convertToCoreMessages(messages),
  });

  // OneTool remains constant regardless of tool count
  // Reduces hallucinations by simplifying tool interface
  // Enables reliable agent interactions at scale

  return stream.toDataStreamResponse();
}

```